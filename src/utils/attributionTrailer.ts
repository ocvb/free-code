/**
 * Attribution trailer builder. No-op stub in this reconstructed snapshot.
 * (The real file contains internal-only strings excluded from external builds.)
 */

export function buildPRTrailers(
  _attributionData: unknown,
  _attribution: unknown,
): string[] {
  // No-op in reconstructed snapshot.
  return []
}
