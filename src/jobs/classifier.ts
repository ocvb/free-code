/**
 * Job classifier stub for TEMPLATES feature.
 * Classifies incoming requests for template job routing.
 */

export async function classifyJob(_input: string): Promise<string | null> {
  // No-op stub — template job classification not available in this snapshot.
  return null
}
